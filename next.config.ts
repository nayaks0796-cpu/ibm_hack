import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // PWA manifest is in public/manifest.json
  // next-intl middleware and routing configured in middleware.ts (step 11)
};

export default nextConfig;
